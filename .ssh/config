# Fallback because not everything supports all terminals
SetEnv TERM=xterm-256color

Host *
  PermitLocalCommand yes
  # Set terminal background for any SSH host
  LocalCommand test -t 1 && printf '\033]11;#2b0000\007' || true
